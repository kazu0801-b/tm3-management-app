import { AppLayout } from "@/components/templates/AppLayout";

export default function CalenderPage() {
    return (
        <AppLayout>
            <h1 className="text-2xl font-bold">カレンダー</h1>
            <p className="mt-4 text-gray-600">カレンダー機能は今後実装予定です。</p>
        </AppLayout>
    );
}